<!--
  M0 carries no per-page surface; the visible UI lives in `+layout.svelte`.
  M2 will mount the chat shell at `/`.
-->
<p class="text-ink-muted tracking-label text-xs uppercase">Page slot reserved for M2.</p>
